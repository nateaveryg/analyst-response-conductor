name: rficonductorv3_client
description: "The Conductor v3 Unified Multi-Platform Flutter Client for Google Cloud Analyst Response Agent."
publish_to: "none"
version: 3.3.1+2

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  pluto_grid: ^8.0.0
  crypto: ^3.0.3
  intl: ^0.19.0
  google_fonts: ^6.2.1
  flutter_markdown: ^0.7.1
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
